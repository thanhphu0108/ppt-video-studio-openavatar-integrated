[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openavatar-sdk"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1"]

[tool.setuptools.packages.find]
include = ["openavatar_sdk*"]
